campaign:
  name: carry_dual_trend_v1_oos_2y_full
  description: >-
    Full robustness validation of the frozen 60/10/30 dual-trend confirmation. No signal, sleeve
    weight, risk constraint or temporal parameter differs from the confirmation trial.
  hypothesis_id: HYP-CARRY-DUAL-TREND-0001
  artifact_policy: summary
  max_trials: 1

dataset:
  manifest: var/data/gold/binance/usdm/research_dataset/version=57deca28711c2c48/dataset.json

feature_set:
  name: phase3_baseline_features
  version: "1"

label:
  name: gross_forward_return_1h
  version: "1"
  horizon_minutes: 60

strategy:
  name: carry_dual_trend
  version: "1"
  fixed:
    funding_rate_weight: 1.0
    funding_change_weight: 0.0
    momentum_confirmation_weight: 0.25
    relative_strength_lookback_hours: 168
    sma_fast_window_hours: 12
    sma_slow_window_hours: 168

portfolio:
  name: buffered_three_sleeve_neutral
  version: "1"
  fixed:
    carry_weight: 0.60
    fast_strength_weight: 0.10
    slow_strength_weight: 0.30
    no_trade_score_band: 1.0
    rebalance_interval_hours: 48
    gross_exposure: 0.50
    annual_volatility_target: 0.15
    max_symbol_weight: 0.25

execution:
  name: bar_next_open
  version: "1"
  fixed: {}

costs:
  name: configured_taker
  version: "1"
  fixed:
    cost_multiplier: 1.0

validation:
  profile: full
  split_plan: expanding_walk_forward_v1
  train_days: 1435
  test_days: 14
  embargo_bars: 1
  stress_cost_multipliers: [1.5, 2.0]
  stress_signal_delay_bars: [1]
  bootstrap_samples: 2000
  bootstrap_block_hours: 24

runner:
  max_workers: 1
  fail_fast: true
  resume: true
