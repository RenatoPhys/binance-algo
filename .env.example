# Public market-data commands do not require credentials.
BINANCE_API_KEY=
BINANCE_API_SECRET=

# The first milestone deliberately rejects either setting when true.
LIVE_TRADING=false
ALLOW_ORDER_SUBMISSION=false

# Optional local override; Demo Trading is the safe default.
# BINANCE_REST_BASE_URL=https://demo-fapi.binance.com
